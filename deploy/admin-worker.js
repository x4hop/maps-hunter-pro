async function page(env){const r=await env.DB.prepare("SELECT chunk FROM static_blobs WHERE name='admin' ORDER BY seq").all(),b64=(r.results||[]).map(x=>x.chunk).join('');if(!b64)return new Response('Admin page not found',{status:500});const bytes=Uint8Array.from(atob(b64),c=>c.charCodeAt(0)),html=await new Response(new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'))).text();return new Response(html,{headers:{'content-type':'text/html;charset=utf-8','cache-control':'no-store','x-frame-options':'DENY','x-content-type-options':'nosniff','referrer-policy':'no-referrer','content-security-policy':"default-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; connect-src 'self'; frame-ancestors 'none'; base-uri 'none'"}})}
export default{async fetch(req,env){const u=new URL(req.url);if(u.pathname.startsWith('/api/'))return env.API.fetch(req);return page(env)}};
